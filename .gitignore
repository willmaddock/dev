# Hugo generated output (public directory where the static site is built)
public/

# Hugo cache and temporary files
resources/_gen/
.hugo_build.lock

# Local environment and IDE files
.idea/                    # GoLand/IntelliJ IDEA project files
.vscode/                  # Visual Studio Code settings
*.sublime-project         # Sublime Text project files
*.sublime-workspace

# Operating system files
.DS_Store                 # macOS Finder metadata
Thumbs.db                 # Windows thumbnail cache

# Dependency directories (if you install tools locally)
node_modules/             # Node.js modules (unlikely for PaperMod, but included for completeness)
vendor/                   # Vendored dependencies (if using Go modules or other tools)

# Sensitive or local configuration files
config.local.toml         # Local Hugo config for development
.env                      # Environment variable files
*.bak                     # Backup files
*.tmp                     # Temporary files

# Logs and temporary files
*.log
tmp/